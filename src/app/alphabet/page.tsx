import { MainLayout } from '@/components/layout/main-layout';
import { AlphabetContent } from '@/components/content/alphabet-content';

export default function AlphabetPage() {
  return (
    <MainLayout>
      <AlphabetContent />
    </MainLayout>
  );
}