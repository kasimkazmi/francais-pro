import { MainLayout } from '@/components/layout/main-layout';
import { GrammarContent } from '@/components/content/grammar-content';

export default function GrammarPage() {
  return (
    <MainLayout>
      <GrammarContent />
    </MainLayout>
  );
}
