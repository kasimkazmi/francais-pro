import { MainLayout } from '@/components/layout/main-layout';
import { NumbersContent } from '@/components/content/numbers-content';

export default function NumbersPage() {
  return (
    <MainLayout>
      <NumbersContent />
    </MainLayout>
  );
}
